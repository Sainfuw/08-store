---
import { getFullImages } from '@utils/images'

interface Props {
  src: string
  alt: string
  className?: string
}

const { src, alt, className } = Astro.props
const currentImage = getFullImages([src]).at(0)
---

<img src={currentImage} alt={alt} class:list={[className]} />
